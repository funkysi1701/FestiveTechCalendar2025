apiVersion: v1
kind: Service
metadata:
  name: {{ include "festivetechcalendar2025.fullname" . }}
  labels:
    {{- include "festivetechcalendar2025.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "festivetechcalendar2025.selectorLabels" . | nindent 4 }}
